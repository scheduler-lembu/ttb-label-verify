Worth creating — these three carry real weight:

ARCHITECTURE.md — the technical blueprint: component diagram, the request/data flow, the batch concurrency model, and the exact file/module layout Cowork builds against. This is high value twice over: it's the thing I need before delegating any code, and "appropriate technical choices" is an explicit grading criterion, so a clear architecture doc is graded evidence. This is the natural next step regardless.
TEST_PLAN.md (+ a test-label catalog) — this is the strongest addition and it's on-brand for my role. "Correctness and completeness of core requirements" is grading criterion #1. A test plan that lists each test label (compliant and deliberately broken — title-case warning, shrunk warning, STONE'S THROW vs Stone's Throw, 90 proof vs 45%, a legitimately ABV-less beer, an unreadable image), with the expected result for each and which requirement it proves, does three things: it forces the matching logic to be correct, it gives the Treasury reviewer a guided way to exercise the app, and it proves we understood the buried requirements. We're generating test labels anyway (ASM-04), so we need to track them somewhere — this is that place.
README.md — not optional; it's a required deliverable (setup/run instructions + approach/tools/assumptions). I flag it because the two working docs we already have are deliberately written to feed it — most of the README is already drafted across REQUIREMENTS and ASSUMPTIONS. We assemble it at the end; almost no new writing.

Worth it for you specifically, but write later when we reach that stage:

BUILD_PLAN.md — a phased roadmap (e.g., Phase 1 single-label core → Phase 2 batch → Phase 3 the bonus font signal → Phase 4 deploy). Its real value for us is coordination: it sequences what Cowork builds in what order, and it doubles as my running status tracker so you're never lost about "what's next." I'd sketch this the moment we finish architecture.
DEPLOYMENT_RUNBOOK.md — exact Docker + Fly.io + GitHub commands, in order, with full Windows paths, written so you never make a judgment call. This is where a non-technical user hits the most friction, so it earns its place — but only when we actually deploy. Premature now.